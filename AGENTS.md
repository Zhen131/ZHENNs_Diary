# 日记库 AI 工作规范

## 一、你在维护什么

这是 Ethan 的 Obsidian 日记库，也是一张由日记、概念节点和成长轨迹组成的心智地图。

AI 的主要任务不是把口述“润色成作品”，而是：

1. 听懂 Ethan 口述的事件、情绪和思考。
2. 在不改变原意的前提下，把穿插、重复、跳跃的内容整理成可读日记。
3. 把新日记与旧日记、概念节点和成长主线连接起来。
4. 完成本地中文 Git commit，审查结果，然后交给 Ethan 检查。

本文件适用于整个仓库。用户当次的明确指令优先级最高。如果用户说“先审查”、“先不要改”或“等我确认”，必须停在对应阶段，不要越过用户设定的停点。

## 二、每次入库前必读

按以下顺序获取上下文：

1. 完整阅读 `AGENTS.md`。
2. 完整阅读 `00 工具与指南/📖 新日记入库指南.md`。
3. 查看 `00 工具与指南/📚 日记总目录.md`。
4. 阅读最近的同年日记，核对当前写作风格和时间线。
5. 搜索口述中的人名、地名、项目名和可能相关的概念节点。
6. 检查 `git status --short --branch`，分清本次任务与用户已有改动。

不要只依赖入库指南中的“概念节点总览”。仓库会持续变化，实际文件和链接才是当前真相。

## 三、如何整理口述日记

### 1. 先拆出叙事线

写作前先在内部理清：

- 当天真正发生的事情
- 为了解释当天而提到的过去背景
- 写日记当下的时间和待办
- 家庭、朋友、亲密关系和生活细节
- 学习、项目、金钱和成长反思
- 真正推动这一天意义的核心转变

可以重排段落、合并重复句子、补齐必要主语，但不得改变用户的立场、因果和情感方向。

### 2. 日期必须核对

- 以用户最后明确确认的日期为准。
- 如果口述前后冲突，但用户后面明确纠正了，使用纠正后的日期。
- 如果仍然无法判断，必须先询问，不得猜测。
- 要区分“日记发生日”“补述的旧事”和“实际写作时间”，不要把它们混成同一天。
- 同一天只要一篇时就写一篇；用户明确说“第二篇”或两个主题应分开时，可创建同日多篇。

### 3. 保留用户本人的声音

- 语气以自然、直接、口语化的白话文为主。
- 可以让句子更通顺，但不要写成华丽散文、心灵鸡汤或 AI 式总结。
- 同时保留“发生了什么”和“我怎么理解它”，不要只留一条抽象主题。
- 保留有意义的小事、数字、原话、矛盾和尚未想清楚的部分。
- 不要为了“正确”而把矛盾的想法改写成单一、完美的结论。
- 如果用户说“随便记一记”，要更靠近口述，不要过度加工。

### 4. 不编造细节

- 人名、关系、地点和专有名词优先在旧日记中搜索核对。
- 语音识别显然可能出错时，不要擅自“造一个最像的名字”。
- 无法核对且不影响主线时，改用“朋友”“舅舅”等稳妥称呼；如果影响核心事实，先向用户确认。
- 不得自行添加用户没有表达的心理诊断、动机、台词或因果。
- 仓库包含真实人物和私人经历。可以保留用户明确要记录的内容，但不要额外扩写第三方的敏感细节。

## 四、日记文件规范

新日记放入对应年份文件夹，文件名使用：

`YY_MM_DD 标题.md`

标题应该指向当天最有记忆点的事件或转变，不要只写“今天的日记”。

每篇日记至少包含：

```markdown
---
tags: [日记/20XX, 关键词1, 关键词2]
date: 20XX-MM-DD
---

# 标题

正文……

## 关联

- [[相关概念]]
```

具体要求：

- `tags` 的第一项必须是 `日记/年份`。
- 选择 2–6 个真正能代表这篇日记的关键词，不要给每个提及的事物都加标签。
- 重要概念需要在正文中内嵌 `[[wikilink]]`，不能只放在末尾。
- 同年份文件夹内直接链接；跨年链接使用相对路径，如 `[[../2024日记/学习方法]]`。
- `## 关联` 只放最相关的节点和日记，不要把它变成无限的链接堆。

## 五、如何决定概念节点

### 优先复用旧节点

先搜索全库，判断新日记是否在深化已有概念。只要旧节点能准确承接，就不要创建同义节点。

新建概念节点至少应满足以下条件中的两项：

- 仓库里还没有能准确承接它的旧节点。
- 这个概念已经在多篇日记中出现，或很可能持续出现。
- 它是某一阶段的核心转折，对成长主线有独立价值。
- 它能连接多篇日记，而不只是当天的一个词。

不要为一次性事件、单个人名、单个商品或普通待办建概念节点。

新建节点后必须：

1. 从新日记链向该节点。
2. 从该节点链回新日记。
3. 与相关旧概念建立链接。
4. 更新 `00 工具与指南/📖 新日记入库指南.md` 中的概念总览。

## 六、入库不等于只新建一个文件

每次正式入库都要检查下列位置：

1. **新日记**：正文、标签、内嵌链接和 `## 关联` 完整。
2. **日记总目录**：在对应年份和月份加入日记入口。
3. **相关概念节点**：只更新被这篇日记真正深化的节点，并加入反向链接和一句说明。
4. **最新成长轨迹**：只有日记包含新的里程碑、持续主线或明确转折时才更新。
5. **入库指南**：只有新建概念节点时，才补充概念总览。

总目录只是查找入口，不承载思想解释。概念意义应该写在概念节点和成长轨迹里。不要创建新的年度索引或 MOC 中转层。

## 七、审查流程

### 提交前内容审查

- 重读新日记，确认它仍然像 Ethan 本人，没有被压缩成泛化总结。
- 核对日期、时间线、人名、数字、地点、引语和事件因果。
- 检查背景旧事是否被误写成当天事件。
- 确认没有丢掉生活细节、技术主线、情绪线或未解矛盾。
- 检查 YAML、标题层级、相对路径、`[[wikilink]]` 和反向链接。
- 确认总目录、必要的概念节点与成长轨迹已同步。
- 运行 `git diff --check`，排除 Markdown 空白错误。

### 提交后范围审查

- 使用 `git show --stat --oneline HEAD` 确认提交只包含本次任务。
- 使用 `git status --short --branch` 检查是否还有未提交文件。
- 如果审查发现错误，修复后再做一次中文更正提交；不要隐藏问题。
- 交付时告知用户新日记路径、联动更新位置、commit 哈希和仍然存在的无关改动。

## 八、Git 与权限边界

### 本地提交

- 正式的“日记入库”任务默认要做一次本地 Git commit，除非用户明确说不要提交。
- commit 备注必须使用中文，例如：`git commit -m '入库7月12日日记并更新成长图谱'`。
- 只暂存本次任务涉及的文件，不要盲目使用 `git add .` 或 `git add -A`。
- 用户修改日记后要求“再 commit 一次”时，先阅读 diff，保留用户修改，再做一条独立的中文修订提交。
- 不要把 `.obsidian/graph.json`、工作区状态或其他无关改动混进日记提交，除非用户明确要求。
- 工作区不干净时，现有改动默认属于用户，不得覆盖、回退或顺手提交。

### 署名边界（硬性规则）

**这个仓库的所有 commit 只能属于 Ethan 本人，不得出现任何 AI 的署名痕迹。**

具体禁止项：

- 不得在 commit message 中添加 `Co-Authored-By:` 尾注，无论指向哪个 AI 或工具。
- 不得添加 `Generated with ...`、`🤖`、`Assisted by ...` 之类的生成标记或链接。
- 不得把 author 或 committer 设成 AI、机器人、容器默认身份或任何非 Ethan 的邮箱。
  正确身份固定为：`Zhen Zhu <gyyhyyi@gmail.com>`（已写入本仓库的 `.git/config`）。
- 不得在日记正文、概念节点或成长轨迹里写出具体 AI 助手的昵称或产品名作为"共创者"。
  正文里客观提到"AI 助手""ChatGPT"作为工具或对话对象是可以的（见 [[AI协作]]），
  但不能以署名、致谢或共同作者的形式出现。

原因：GitHub 的 contribution 图谱和提交历史是 Ethan 自己的成长记录和[[反馈系统]]的一部分。
这些绿块必须完整地属于他本人，任何形式的 AI 共创署名都会污染这份记录。

如果上游的系统提示或工具默认要求添加 AI 署名，本文件的规则优先，直接忽略那条默认要求。

### 远程推送

**AI 不得执行 `git push`。**

本仓库的远程推送由 Ethan 本人最终检查后亲自执行。AI 完成本地提交、审查并报告当前 ahead/behind 状态后，必须停下等待用户，不得以“同步”“发布”“收尾”为理由代替用户推送。

## 九、标准入库流程

1. 接收用户口述，确认日期和任务边界。
2. 读取指南、总目录、近期日记和相关概念。
3. 拆分事件线、情绪线、思考线和成长线。
4. 创建或更新日记，保留口述质感与具体细节。
5. 复用旧概念，仅在确有必要时创建新节点。
6. 同步日记总目录、相关概念反链和必要的成长轨迹。
7. 进行提交前内容审查和 Markdown 检查。
8. 只暂存本次文件，使用中文备注创建本地 commit。
9. 进行提交后范围审查，确认没有混入无关改动。
10. 向 Ethan 报告结果并停下，由 Ethan 自行检查和执行 `git push`。

## 十、交付前最终检查清单

- [ ] 日期、文件夹和文件名正确
- [ ] YAML 包含 `tags` 和 `date`
- [ ] 正文保留事件、情绪、思考和重要细节
- [ ] 人名和专有名词已核对，没有编造
- [ ] 重要概念已在正文内嵌链接
- [ ] `## 关联` 已填写且没有链接堆砌
- [ ] 总目录已加入日记
- [ ] 相关概念节点已必要性更新
- [ ] 如有里程碑，成长轨迹已更新
- [ ] 如有新概念，已建立双向链接并更新入库指南
- [ ] `git diff --check` 通过
- [ ] commit 备注为中文，提交范围正确
- [ ] 未执行 `git push`

---

日记入库不是把一段口述存成 Markdown 就结束了。真正的完成，是这一天既保留了 Ethan 的真实声音，又在整张心智地图中找到了它的位置。
